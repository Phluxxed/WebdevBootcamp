<h1>Index Page!</h1>

<% blogs.forEach ((blog) => { %>
    <div>
        <h2><%= blog.title %></h2>
        <img src="<%= blog.image %>" style="width: 80%">
        <span><%= blog.created %></span>
        <p><%= blog.body %></p>
    </div>
<% }); %>